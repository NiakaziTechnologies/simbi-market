// Forgot password page doesn't need any layout wrapper - it's a standalone page
export default function ForgotPasswordLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
